---
title: Autosuggest
description: A text input that displays a dropdown of suggestions as the user types.
tag: cs-autosuggest
---
import ComponentPreview from '../../components/ComponentPreview.astro';
import PropsTable from '../../components/PropsTable.astro';

## Preview

<ComponentPreview label="Basic autosuggest">
  <cs-autosuggest id="demo-autosuggest" placeholder="Search..."></cs-autosuggest>
</ComponentPreview>

<script>{`
  const el = document.getElementById('demo-autosuggest');
  if (el) {
    el.options = [
      { value: 'EC2 Instances' },
      { value: 'S3 Buckets' },
      { value: 'Lambda Functions' },
      { value: 'RDS Databases' },
    ];
  }
`}</script>

> **Note:** Options are set via the `options` JavaScript property. See usage below.

<ComponentPreview label="With empty text">
  <cs-autosuggest id="demo-autosuggest-empty" placeholder="Type to search" empty="No matches found"></cs-autosuggest>
</ComponentPreview>

<script>{`
  const el = document.getElementById('demo-autosuggest-empty');
  if (el) {
    el.options = [
      { value: 'CloudWatch Metrics' },
      { value: 'CloudTrail Logs' },
      { value: 'Config Rules' },
    ];
  }
`}</script>

## Usage

```html
<cs-autosuggest placeholder="Search..."></cs-autosuggest>

<script>
  const autosuggest = document.querySelector('cs-autosuggest');
  autosuggest.options = [
    { value: 'Option 1' },
    { value: 'Option 2' },
    { value: 'Option 3' },
  ];
</script>
```

## Properties

<PropsTable component="autosuggest" />
