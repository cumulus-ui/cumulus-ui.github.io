---
description: A dynamic form that lets users add, edit, and remove key-value attribute rows.
tag: cs-attribute-editor
---
import ComponentPreview from '../../components/ComponentPreview.astro';
import PropsTable from '../../components/PropsTable.astro';

## Preview

<ComponentPreview label="Basic">
  <cs-attribute-editor id="demo-attr-editor" add-button-text="Add new tag"></cs-attribute-editor>
</ComponentPreview>

<script>{`
  const el = document.getElementById('demo-attr-editor');
  if (el) {
    el.definition = [
      { label: 'Key', control: item => item.key },
      { label: 'Value', control: item => item.value },
    ];
    el.items = [
      { key: 'Environment', value: 'Production' },
      { key: 'Team', value: 'Frontend' },
    ];
  }
`}</script>

<ComponentPreview label="Empty">
  <cs-attribute-editor id="demo-attr-empty" add-button-text="Add new tag"></cs-attribute-editor>
</ComponentPreview>

<script>{`
  const empty = document.getElementById('demo-attr-empty');
  if (empty) {
    empty.definition = [
      { label: 'Key', control: item => item.key },
      { label: 'Value', control: item => item.value },
    ];
    empty.items = [];
  }
`}</script>

<ComponentPreview label="With description">
  <cs-attribute-editor id="demo-attr-desc" add-button-text="Add new tag">
    <cs-header slot="header" variant="h3" description="Tags help you manage, identify, and organize resources." counter="(2)">Tags</cs-header>
  </cs-attribute-editor>
</ComponentPreview>

<script>{`
  const desc = document.getElementById('demo-attr-desc');
  if (desc) {
    desc.definition = [
      { label: 'Key', control: item => item.key },
      { label: 'Value', control: item => item.value },
    ];
    desc.items = [
      { key: 'Environment', value: 'Production' },
      { key: 'CostCenter', value: 'CC-1234' },
    ];
  }
`}</script>

## Usage

The `items` and `definition` properties must be set via JavaScript:

```html
<cs-attribute-editor add-button-text="Add new tag"></cs-attribute-editor>

<script>
  const editor = document.querySelector('cs-attribute-editor');
  editor.definition = [
    { label: 'Key', control: (item) => item.key },
    { label: 'Value', control: (item) => item.value },
  ];
  editor.items = [
    { key: 'Environment', value: 'Production' },
    { key: 'Team', value: 'Frontend' },
  ];
</script>
```

## Properties

<PropsTable component="attribute-editor" />
